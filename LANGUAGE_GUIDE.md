# نظام الترجمة المتعدد اللغات (i18n)

## ✅ اللغات المدعومة:
- **AR** 🇸🇦 العربية (RTL - من اليمين إلى اليسار)
- **EN** 🇬🇧 الإنجليزية (LTR - من اليسار إلى اليمين)  
- **FR** 🇫🇷 الفرنسية (LTR - من اليسار إلى اليمين)

## 🎯 ميزات النظام:

### 1️⃣ التبديل الفوري بين اللغات
```html
<button class="lang-btn" data-lang="ar">العربية</button>
<button class="lang-btn" data-lang="en">English</button>
<button class="lang-btn" data-lang="fr">Français</button>
```

### 2️⃣ تغيير الاتجاه تلقائياً (RTL/LTR)
عند اختيار لغة، يتغير اتجاه الصفحة تلقائياً:
- العربية (AR) → RTL (من اليمين)
- الإنجليزية (EN) → LTR (من اليسار)
- الفرنسية (FR) → LTR (من اليسار)

### 3️⃣ حفظ اللغة المختارة
اللغة المختارة تُحفظ في `localStorage` وتُستعاد عند زيارة الموقع مجدداً

### 4️⃣ تحديث النصوص الفوري
جميع العناصر ذات `data-i18n` تُحدّث نصوصها فوراً عند تغيير اللغة

## 📝 كيفية إضافة ترجمات:

### للنصوص الثابتة:
```html
<h1 data-i18n="welcome_title">مرحبا بك في Wed in Click</h1>
<p data-i18n="main_tagline">منصتك الشاملة...</p>
```

### للـ Placeholders في الحقول:
```html
<input 
  type="text" 
  placeholder="الاسم"
  data-i18n-placeholder="name"
>
```

### للعناوين والـ Titles:
```html
<button 
  title="تسجيل الدخول"
  data-i18n-title="login"
>
  دخول
</button>
```

## 📚 قائمة المفاتيح المتاحة:

### الترحيب والمصادقة:
- `welcome_title` - العنوان الرئيسي
- `welcome_sub` - الوصف الفرعي
- `auth_title` - عنوان المصادقة
- `auth_sub` - وصف المصادقة
- `privacy` - سياسة الخصوصية

### النماذج:
- `name` - الاسم
- `email` - البريد الإلكتروني
- `password` - كلمة المرور
- `confirm_password` - تأكيد كلمة المرور
- `phone` - رقم الهاتف
- `location` - المحافظة

### الأزرار والإجراءات:
- `register` - إنشاء حساب
- `login` - تسجيل الدخول
- `logout` - تسجيل الخروج
- `start_now` - ابدأ الآن
- `browse_services` - تصفح الخدمات

### الخدمات:
- `venue` - قاعة الأفراح
- `photographer` - المصور
- `catering` - الطعام والشراب
- `decoration` - الديكور

### الواجهة العامة:
- `home` - الرئيسية
- `services` - الخدمات
- `search` - البحث
- `about` - عن المنصة
- `contact` - اتصل بنا
- `profile` - الملف الشخصي
- `settings` - الإعدادات
- `language` - اللغة
- `dark_mode` - الوضع المظلم

### الرسائل:
- `success` - نجح
- `error` - خطأ
- `loading` - جاري التحميل
- `email_required` - البريد مطلوب
- `password_required` - كلمة المرور مطلوبة
- `email_exists` - البريد موجود بالفعل
- `invalid_email` - بريد غير صحيح
- `weak_password` - كلمة مرور ضعيفة

## 🔧 كيفية إضافة ترجمة جديدة:

1. افتح `main.js`
2. ابحث عن قسم `window.i18n`
3. أضف المفتاح الجديد للغات الثلاث:

```javascript
ar: {
    // الخاص بك
    my_new_key: 'النص بالعربية',
},
en: {
    my_new_key: 'English text',
},
fr: {
    my_new_key: 'Texte français',
}
```

4. في HTML، استخدم:
```html
<span data-i18n="my_new_key">النص بالعربية</span>
```

## 🎨 أمثلة عملية:

### تغيير اللغة برمجياً:
```javascript
// تغيير إلى اللغة الإنجليزية
window.i18n.setLang('en');

// الحصول على اللغة الحالية
const currentLang = window.i18n.current;

// الحصول على ترجمة معينة
const welcomeText = window.i18n.t('welcome_title');
```

### الاستماع لتغيير اللغة:
```javascript
window.addEventListener('languageChanged', (e) => {
    const newLang = e.detail.lang;
    console.log('تم تغيير اللغة إلى:', newLang);
    // قم بأي تحديثات إضافية هنا
});
```

## ✨ نقاط مهمة:

✅ جميع التحديثات فورية ولا تحتاج إعادة تحميل صفحة
✅ اللغة تُحفظ في localStorage وتبقى عند إعادة الزيارة
✅ اتجاه الصفحة يتغير تلقائياً حسب اللغة
✅ جميع الصور والأيقونات تدعم RTL/LTR
✅ نظام معياري يمكن توسيعه بسهولة

## 🚀 التطوير المستقبلي:

- إضافة لغات جديدة
- دعم تنسيقات تاريخ وأرقام محلية
- تخزين تفضيلات المستخدم
- نظام ترجمة متقدم مع API خارجي
